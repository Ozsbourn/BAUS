import '@mantine/core/styles.css';

export const metadata = {
  title: 'a/g/dash/',
  description: 'desc',
};

export default function GroupDashboardLayout({ children }: { children: any }) {
  return <div>{children}</div>;
}
