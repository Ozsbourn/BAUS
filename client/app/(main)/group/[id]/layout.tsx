import '@mantine/core/styles.css';

export const metadata = {
  title: 'a/g/:id',
  description: 'desc',
};

export default function GroupPageLayout({ children }: { children: any }) {
  return <div>{children}</div>;
}
