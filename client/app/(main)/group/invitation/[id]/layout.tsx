import '@mantine/core/styles.css';

export const metadata = {
  title: 'a/i/',
  description: 'desc',
};

export default function InvitationPageLayout({ children }: { children: any }) {
  return <div>{children}</div>;
}
