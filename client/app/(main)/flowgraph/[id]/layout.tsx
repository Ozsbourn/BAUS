import '@mantine/core/styles.css';

export const metadata = {
  title: 'a/fg/:id',
  description: 'desc',
};

export default function FlowGraphLayout({ children }: { children: any }) {
  return <div>{children}</div>;
}
